{% extends 'base.html.twig' %}

{% block body %}
<h1>Cancel Services</h1>

<p>Welcome, {{ user.name }}</p>
<p>Email: {{ user.email }}</p>

{{ form_start(form) }}
    {{ form_widget(form) }}
{{ form_end(form) }}

{% for message in app.flashes('error') %}
    <div class="error">{{ message }}</div>
{% endfor %}
{% endblock %}
